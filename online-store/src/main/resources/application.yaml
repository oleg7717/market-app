spring:
  application:
    name: market
  profiles:
    active: dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
  data:
    redis:
      host: localhost
      port: 6379